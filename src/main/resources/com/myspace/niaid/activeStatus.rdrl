package com.myspace.niaid;

import java.lang.Number;

rule "activeStatus"
	dialect "mvel"
	when
		emp : employee( effectiveSdate <= ( LocalDate.now() ) )
	then
		 System.out.println("out of date");
end
